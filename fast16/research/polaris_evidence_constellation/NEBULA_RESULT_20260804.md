# 北极星星云相 / Reframe v0 首次结果

日期：2026-08-04
性质：纯 CPU、秒级、冻结夹具结构门

## 结果

- 定向单元门：`12/12` 通过（包含原证据核心 `6/6`）；
- 输入完整候选列表：`0`；
- 初始全局冲突：`4`；
- Reframe 后组内冲突：`0`；
- 选出的新坐标：`workload`；
- 新坐标形成三个执行平面：`request / batch / interaction`；
- 外部证据到达前提交的新结构：`0`；
- 外部回执闭合后提交的局部结构：`3`；
- 模型、GPU、训练：均为 `0`。

## 这次真正增加了什么

以前的内核从已经命名的 `Claim/Proposal` 开始。本轮在其前面增加星云相：每个器官只给出
自己的局部 `context + assertion`，没有任何器官给出完整的三平面架构。Reframe 使用最小
描述长度比较不同问题划分，选择能减少矛盾、又不会把每条观测切成孤岛的坐标。

本例中，按部署位置划分仍会留下矛盾并产生大量孤立组；按工作负载划分则把原本互相冲突的
PHP、JVM、JavaScript 解释为不同执行平面的局部实现。因此系统没有回答“全局选哪种语言”，
而是改变了问题本身的划分方式。

## 防自欺检查

1. 将 `workload/deployment` 改成无语义的 `axis_z/axis_a` 后仍选择相同关系结构；
2. 改变碎片间关系后，系统会改选 `deployment`，说明没有硬编码选择 `workload`；
3. 每个冲突值只有一个独立上下文时，系统拒绝用全单例分区记忆答案；
4. 全局没有冲突时，系统拒绝为了“新颖”强行 Reframe；
5. 暂态 Frame 进入现有证据星座后，在 runtime 回执到达前保持 pending。

## 没有证明什么

- 局部上下文和断言仍由人工夹具提供，没有证明神经模型会自主形成有价值的前概念活动；
- 当前 Reframe 只能从已出现的上下文维度中选择或组合，尚不能发明完全未命名的新维度；
- 最小描述长度的成本系数尚未从真实任务校准；
- 结构门通过不是聊天能力、意识、模型质量或论文新颖性的证据。

机器回执：`nebula_gate_receipt.json`。
