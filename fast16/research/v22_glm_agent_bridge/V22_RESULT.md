# v22 GLM Agent Bridge 独立结果

## 结论

GLM 深层桥方向泛化真实存在，但幅度误差连续三次未过预先固定的 `NRMSE <= 0.95` 门。
v22 正式停止，不实现 C++ 连续岛。

## 独立数据

- 16 条 Agent/工具/状态恢复提示在看结果前冻结。
- 提示文件 SHA-256：
  `2438f52b8aca7b943769ff08b1f9d0a7bb60b8603f341272e7d6767e2d5a8362`。
- ColorLM L35 与 GLM L42 共配对 1227 个状态，16/16 提示成功，平均覆盖率 99.25%。

## 冻结 v21 岭桥外部结果

- 余弦中位数：`0.0074 -> 0.7405`。
- 16/16 提示方向提升。
- NRMSE：`1.0166 -> 0.9832`，比率 `0.9671`，失败。

这证明旧结果不是只记住原12条提示；真正残余问题是尺度/幅度，不是方向或prompt泛化。

## 唯一预声明仿射候选

- 偏置、正则、尺度全部只用旧 v21 的766个状态拟合或选择。
- 新1227状态只作一次外部评估，没有参与调参。
- 余弦中位数：`0.0074 -> 0.4216`，16/16提示仍提升。
- NRMSE比率：`0.9809`，比冻结岭桥更差，失败。

因此不再增加第三种GLM桥、不再扫描阈值、不写运行图。GLM整模的标准工具调用改善仍作为
供体能力证据保留，但当前无法稳定接入ColorLM坐标。

## 下一供体约束

Kimi K3与DeepSeek-V4虽强，但本机无法运行完整文本干来产生真实深层状态；继续拆单专家会
重走v20失败路线。下一供体改为能在32GiB内存完整运行的Fara1.5-27B：先验证整模的截图/工具
互补能力，再采真实深层状态。它与ColorLM词表同为248,320，且MIT许可，适合作为电脑操作岛。
