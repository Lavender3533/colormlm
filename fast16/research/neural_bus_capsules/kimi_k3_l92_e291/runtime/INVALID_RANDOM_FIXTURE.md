# 禁止用于模型运行

此目录中的六个 `.f16` 文件是固定 seed 42、标准差 0.02 的随机编译夹具，
不是 Kimi K3 权重。

- 缺少 `capsule.json` 来源契约。
- `norm.f16` 与已下载的 K3 第 92 层 `routed_expert_norm` 不一致。
- 只能用于检查文件尺寸和加载器拒绝路径，不能用于能力实验或版本发布。

正式 v9 启动器必须要求带完整 SHA-256 来源链的真实宏胶囊目录。
