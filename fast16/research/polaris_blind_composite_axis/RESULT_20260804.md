# Blind Composite-Axis Gate v1 首次结果

日期：2026-08-04

## 结论

七个预先固定、彼此不同的隐藏世界全部通过。它们覆盖四个二器官复合坐标和三个三器官
复合坐标；发现器只读取 discovery，随后由独立 evaluator 解封 holdout 与干预结果。

- 定向合同测试：`5/5`；
- 多 seed 盲门：`7/7`；
- 七个隐藏坐标：`7/7` 不同；
- held-out 准确率：全部 `100%`；
- 最佳单器官准确率：`46.88%–52.73%`；
- 联合 Frame 相对单器官提升：`47.27–53.13` 个百分点；
- 删除任一必要器官后的最小下降：`45.70–49.80` 个百分点；
- 干预一致性：全部 `100%`；
- 64 次标签置换的平均信息准确率：`54.53%–59.01%`；
- 标签置换偶然恢复目标/取反的比例：`6.25%–12.50%`，低于按 20 个唯一候选函数
  计算的三标准差机会界 `21.25%`；
- 总门耗时：约 `5.62 秒`；
- 模型、GPU、下载、训练：均为 `0`。

## 证明范围

在已知的关系语法内，系统可以从多个器官的分散原始量中合成一个输入没有直接命名的
复合布尔坐标。任一单器官不能预测结果，联合表达式能在未见 episode 和独立干预上泛化。
这比 v0 的“从 workload/deployment 中选择已有字段”严格前进了一步。

## 未证明范围

- 表达式语法仍由人规定；本轮只覆盖器官内 `<` 和跨器官二/三项 parity；
- 隐藏世界是合成生成器，不是真实模型激活或现实工程状态；
- 穷举搜索 20 个唯一函数不是可扩展的通用认知算法；
- 结果不证明聊天能力、意识、超过 Transformer 或论文新颖性；
- 下一步应只做一次真实日志盲门，不应继续增加更多人工合成夹具。

完整机器回执：`blind_composite_gate_receipt.json`。
